services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: kimngeam
      POSTGRES_USER: kimngeam
      POSTGRES_PASSWORD: kimngeam
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
